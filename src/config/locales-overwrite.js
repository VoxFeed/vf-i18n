// These locales will overwrite the original ones because formattign is better.

const TIME_LOCALES = {
  es: 'en'
};

const NUMBER_LOCALES = {
  es: 'en'
};

module.exports = {
  TIME_LOCALES,
  NUMBER_LOCALES
};
